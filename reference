* For Following users: https://api.twitter.com/1.1/friendships/create.json?user_id=1401881&follow=true
T.post('friendships/create', { id: '370742121' },  function (err, data, response) {
  console.log(data)
})


* To check whether user follows us, use `friendships/lookup` and write up to 100 screen names.


* Getting results from json:
T.post('friendships/destroy', { screen_name: 'yogyyata' }, function (err, data, response) {
  var g = data;
  var result = g.name;
  console.log(result);
});


* Rate limit exceeded means you used up 100 api calls in an hour. Wait for the next hour.


* If you already follow, unfollow them after 3 days
setTimeout(function () {
  // UnFollow
  T.post('friendships/destroy', { screen_name: screen_name },  function (err, data, response) {
    if(!err){
      console.log("Wesbos follower "+screen_name+" unfollowed.")
    } else{
      console.log(err);
    }
  });
}, 1000*60*60*24*3); // 3 days


* Save data to Firebase:
firebase.database().ref("followed_followers_of").child("wesbos").set({
  pepina: {
    date_of_birth: "June 23, 1912",
    full_name: "Alan Turing"
  },
  yogyyata: {
    date_of_birth: "December 9, 1906",
    full_name: "Grace Hopper"
  }
});


* Fake data seeder
firebase.database().ref("followed_followers_of").child("wesbos").update({
  Yogyyata: {
    connection: "followed"
  }
});
firebase.database().ref("to_unfollow").child("wesbos").update({
  Yogyyata: {
    connection: "idk"
  }
});

* Links
https://firebase.google.com/
https://apps.twitter.com/
https://nodejs.org/en/download/
